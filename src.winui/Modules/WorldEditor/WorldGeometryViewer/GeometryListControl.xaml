<UserControl
    x:Class="OmegaAssetStudio.WinUI.Modules.WorldEditor.WorldGeometryViewer.GeometryListControl"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <Grid RowSpacing="12">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <StackPanel Spacing="6">
            <TextBlock FontSize="18"
                       FontWeight="SemiBold"
                       Text="Geometry List" />
            <TextBlock Opacity="0.78"
                       TextWrapping="Wrap"
                       Text="Load and filter zone geometry entries." />
        </StackPanel>

        <StackPanel Grid.Row="1" Spacing="8">
            <TextBlock Text="Category Filter" />
            <ComboBox SelectedIndex="0"
                      ItemsSource="{x:Bind ViewModel.CategoryFilters, Mode=OneWay}"
                      SelectedItem="{x:Bind ViewModel.MeshCategoryFilter, Mode=TwoWay}" />
            <Button Content="Load Geometry"
                    Click="LoadGeometry_Click" />
            <TextBlock Text="{x:Bind ViewModel.StatusText, Mode=OneWay}"
                       Opacity="0.78"
                       TextWrapping="Wrap" />
        </StackPanel>

        <ListView Grid.Row="2"
                  ItemsSource="{x:Bind ViewModel.Geometry, Mode=OneWay}"
                  SelectedItem="{x:Bind ViewModel.SelectedGeometry, Mode=TwoWay}">
            <ListView.ItemTemplate>
                <DataTemplate>
                    <StackPanel Spacing="4" Padding="0,4">
                        <TextBlock Text="{Binding Name}" FontWeight="SemiBold" />
                        <TextBlock Text="{Binding Category}" Opacity="0.72" />
                        <TextBlock Text="{Binding MeshPath}" Opacity="0.72" TextWrapping="Wrap" />
                    </StackPanel>
                </DataTemplate>
            </ListView.ItemTemplate>
        </ListView>
    </Grid>
</UserControl>

