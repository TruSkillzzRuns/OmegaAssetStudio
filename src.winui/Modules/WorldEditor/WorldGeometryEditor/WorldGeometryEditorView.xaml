<Page
    x:Class="OmegaAssetStudio.WinUI.Modules.WorldEditor.WorldGeometryEditor.WorldGeometryEditorView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:local="using:OmegaAssetStudio.WinUI.Modules.WorldEditor.WorldGeometryEditor"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <Grid ColumnSpacing="16">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="360" />
            <ColumnDefinition Width="*" />
        </Grid.ColumnDefinitions>

        <Border Padding="16"
                CornerRadius="16"
                Background="{ThemeResource CardBackgroundFillColorDefaultBrush}"
                BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}"
                BorderThickness="1">
            <Grid RowSpacing="12">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="*" />
                </Grid.RowDefinitions>

                <StackPanel Spacing="6">
                    <TextBlock FontSize="18"
                               FontWeight="SemiBold"
                               Text="Geometry Editor" />
                    <TextBlock Opacity="0.78"
                               TextWrapping="Wrap"
                               Text="Edit world geometry placement on the client side." />
                </StackPanel>

                <StackPanel Grid.Row="1" Spacing="10">
                    <Button Content="Load Selection"
                            Click="LoadSelection_Click" />
                    <Button Content="Apply Transform"
                            Click="ApplyTransform_Click" />
                    <Button Content="Duplicate"
                            Click="Duplicate_Click" />
                    <Button Content="Delete"
                            Click="Delete_Click" />
                    <TextBlock Text="{x:Bind ViewModel.StatusText, Mode=OneWay}"
                               Opacity="0.78"
                               TextWrapping="Wrap" />
                </StackPanel>
            </Grid>
        </Border>

        <Border Grid.Column="1"
                Padding="16"
                CornerRadius="16"
                Background="{ThemeResource CardBackgroundFillColorDefaultBrush}"
                BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}"
                BorderThickness="1">
            <Grid RowSpacing="12">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="*" />
                </Grid.RowDefinitions>

                <StackPanel Spacing="6">
                    <TextBlock FontSize="18"
                               FontWeight="SemiBold"
                               Text="Transform Preview" />
                    <TextBlock Opacity="0.78"
                               TextWrapping="Wrap"
                               Text="Adjust placement values before exporting modified geometry." />
                </StackPanel>

                <local:TransformGizmoControl Grid.Row="1" />
            </Grid>
        </Border>
    </Grid>
</Page>

